=============================================================
DRAG YOUR IMAGES INTO THIS FOLDER. Exact file names matter.
=============================================================

1) THE TURTLE PICTURE (reused everywhere)
   File name:  turtle.png

   This one picture is reused in two places on the site:
     - the little mark at the top of the reveal card
     - the floating turtles that rain down when she clicks Yes

   Tips:
     - PNG with a transparent background looks cleanest.
     - A simple illustration reads better than a photo
       (especially when it shrinks for the floating animation).
     - Square crop, ~400-800 px is plenty.
     - If this file is missing, the site automatically falls back
       to a built-in turtle drawing. Nothing will break.

2) THE THREE POLAROIDS (optional)
   File names:
     photo-1.jpeg   (left polaroid, tilted left)
     photo-2.jpeg   (center polaroid, raised)
     photo-3.jpeg   (right polaroid, tilted right)

   Tips:
     - Square-ish crops look best (they render as squares).
     - If any are missing, the slot shows a soft sage gradient
       instead.  Nothing will break.
     - ~600-900 px per side is plenty.

That's everything. Drop your files in and refresh the page.
